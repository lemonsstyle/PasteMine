# 📚 PasteMine 版本管理指南

## 🎯 版本控制系统：Git

本项目使用 Git 进行版本控制，这是业界标准，非常强大和可靠。

---

## 🚀 快速开始

### 查看当前状态
```bash
cd /Users/lemonstyle/Documents/xcode_pj/pas
git status
```

### 查看提交历史
```bash
git log --oneline --graph --all
```

### 查看所有版本标签
```bash
git tag
```

---

## 🌿 工作流程

### 1️⃣ 开发新功能（推荐方式）

#### 创建新分支
```bash
# 为新功能创建分支
git checkout -b feature/图片支持
# 或
git checkout -b feature/快捷键自定义
```

#### 在新分支上开发
```bash
# 修改代码...

# 查看修改
git status
git diff

# 提交修改
git add .
git commit -m "✨ 添加图片支持功能"
```

#### 测试通过后合并到主分支
```bash
# 切换回主分支
git checkout main

# 合并新功能
git merge feature/图片支持

# 删除功能分支（可选）
git branch -d feature/图片支持
```

---

### 2️⃣ 保存当前稳定版本

#### 方法 A：提交更改
```bash
# 查看修改
git status

# 添加所有修改
git add .

# 提交
git commit -m "🐛 修复快捷键在某些情况下不响应的问题"
```

#### 方法 B：创建版本标签
```bash
# 标记稳定版本
git tag -a v1.0.1 -m "修复快捷键问题"

# 查看所有标签
git tag
```

---

### 3️⃣ 回退到之前的版本

#### 查看历史版本
```bash
git log --oneline
```

#### 临时查看旧版本（不影响当前代码）
```bash
# 切换到某个版本
git checkout v1.0.0

# 查看代码...

# 返回最新版本
git checkout main
```

#### 永久回退到旧版本
```bash
# ⚠️ 警告：这会丢弃之后的所有修改！
git reset --hard v1.0.0
```

#### 回退但保留修改
```bash
# 回退但保留修改到工作区
git reset --soft v1.0.0
```

---

## 🔖 版本标签管理

### 查看所有标签
```bash
git tag
```

### 查看标签详情
```bash
git show v1.0.0
```

### 创建新标签
```bash
# 轻量标签
git tag v1.0.1

# 附注标签（推荐）
git tag -a v1.0.1 -m "版本说明"
```

### 删除标签
```bash
git tag -d v1.0.1
```

---

## 📂 分支管理

### 查看所有分支
```bash
git branch -a
```

### 创建新分支
```bash
git branch feature/新功能名称
```

### 切换分支
```bash
git checkout feature/新功能名称
```

### 创建并切换（快捷方式）
```bash
git checkout -b feature/新功能名称
```

### 删除分支
```bash
# 删除已合并的分支
git branch -d feature/新功能名称

# 强制删除（即使未合并）
git branch -D feature/新功能名称
```

---

## 🎯 推荐的开发流程

### 场景 1：添加新功能

```bash
# 1. 确保主分支是最新的
git checkout main

# 2. 创建功能分支
git checkout -b feature/图片支持

# 3. 开发新功能
# ... 编写代码 ...

# 4. 提交代码
git add .
git commit -m "✨ 添加图片复制支持"

# 5. 测试功能
# ... 测试 ...

# 6. 如果测试通过，合并到主分支
git checkout main
git merge feature/图片支持

# 7. 标记新版本
git tag -a v1.1.0 -m "添加图片支持"

# 8. 删除功能分支
git branch -d feature/图片支持
```

### 场景 2：修复 Bug

```bash
# 1. 创建修复分支
git checkout -b fix/粘贴失败问题

# 2. 修复 bug
# ... 修改代码 ...

# 3. 提交
git add .
git commit -m "🐛 修复在某些应用中粘贴失败的问题"

# 4. 合并到主分支
git checkout main
git merge fix/粘贴失败问题

# 5. 标记补丁版本
git tag -a v1.0.1 -m "修复粘贴问题"
```

### 场景 3：实验性功能（不确定是否要保留）

```bash
# 1. 创建实验分支
git checkout -b experimental/AI功能

# 2. 开发实验功能
# ... 编写代码 ...

# 3. 提交到实验分支
git add .
git commit -m "🧪 实验：添加 AI 智能建议"

# 4. 如果功能好，合并到主分支
git checkout main
git merge experimental/AI功能

# 5. 如果功能不好，直接删除分支
git branch -D experimental/AI功能
```

---

## 💡 提交信息规范

使用清晰的提交信息帮助追踪变更：

```bash
# 新功能
git commit -m "✨ 添加快捷键自定义功能"

# Bug 修复
git commit -m "🐛 修复窗口无法关闭的问题"

# 性能优化
git commit -m "⚡️ 优化数据库查询性能"

# 文档更新
git commit -m "📝 更新安装说明"

# 重构代码
git commit -m "♻️ 重构剪贴板监听服务"

# 测试
git commit -m "✅ 添加自动粘贴单元测试"
```

---

## 🌐 可选：推送到远程仓库

### GitHub（推荐）

```bash
# 1. 在 GitHub 上创建仓库
# 访问 https://github.com/new

# 2. 添加远程仓库
git remote add origin https://github.com/你的用户名/PasteMine.git

# 3. 推送代码
git push -u origin main

# 4. 推送标签
git push --tags
```

### 之后的推送
```bash
# 推送最新代码
git push

# 推送特定分支
git push origin feature/新功能

# 推送标签
git push --tags
```

---

## 🔒 备份策略

### 本地备份（基础）
```bash
# 复制整个项目文件夹
cp -R /Users/lemonstyle/Documents/xcode_pj/pas ~/Desktop/PasteMine_备份_$(date +%Y%m%d)
```

### Git 备份（推荐）
```bash
# 推送到 GitHub（自动备份）
git push

# 或推送到多个远程仓库
git remote add backup https://gitlab.com/你的用户名/PasteMine.git
git push backup main --all --tags
```

### 定期创建备份分支
```bash
# 创建备份分支
git branch backup/$(date +%Y%m%d)

# 查看所有备份
git branch | grep backup
```

---

## 📊 实用命令速查

```bash
# 查看修改
git status
git diff

# 查看历史
git log --oneline --graph
git log --author="你的名字"

# 比较版本
git diff v1.0.0 v1.1.0

# 搜索提交
git log --grep="修复"

# 查看某个文件的历史
git log -- PasteMine/PasteMine/Managers/HotKeyManager.swift

# 恢复误删的文件
git checkout -- 文件名

# 撤销最后一次提交（保留修改）
git reset --soft HEAD~1

# 查看某个版本的某个文件
git show v1.0.0:README.md
```

---

## 🎓 最佳实践

1. **频繁提交**：小步快跑，每完成一个小功能就提交
2. **清晰的提交信息**：让未来的自己能看懂
3. **使用分支**：主分支永远保持稳定
4. **定期打标签**：标记每个稳定版本
5. **推送到远程**：GitHub 是最好的备份
6. **测试后再合并**：确保主分支始终可用
7. **保持 .gitignore 更新**：不提交构建产物

---

## 🆘 常见问题

### Q: 不小心修改了代码，想恢复？
```bash
# 恢复单个文件
git checkout -- 文件名

# 恢复所有文件
git checkout .
```

### Q: 想看之前某个版本的代码？
```bash
git checkout v1.0.0
# 查看完后返回
git checkout main
```

### Q: 提交了但想修改提交信息？
```bash
git commit --amend -m "新的提交信息"
```

### Q: 如何查看两个版本之间的差异？
```bash
git diff v1.0.0 v1.1.0
```

---

## 📞 获取帮助

```bash
# Git 帮助文档
git help
git help commit
git help branch

# 在线资源
# https://git-scm.com/doc
# https://github.com/git-tips/tips
```

---

**记住：有了 Git，您可以随意实验，永远不用担心代码丢失！** 🎉

